[project]
name = "my-mcp-server"
version = "1.0.0"
description = "My MCP Server for StepFun"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
my-mcp-server = "my_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
